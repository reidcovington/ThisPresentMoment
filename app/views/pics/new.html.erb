<%= form_for @pic do |f| %>
  <% if @pic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pic.errors.count, "error") %> prohibited this pic from being saved:</h2>
      <ul>
      <% @pic.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


<div class="pic-container">
        <center>
        <div class="name"><span>This Present Moment</span> </div>
      </center>
        <center>
            <div class="pic-row">
                <div data-new-pics class="pic-holder">
                  <br>
                        <%= f.file_field :image %>
                        Your Location: <%= f.text_field :location %>
                      <div class="actions">
                        <%= f.submit 'Share Moment', :class => 'button-blue add-pic' %>
                      </div>
                    <% end %>
                </div>
            </div>
        </center><br><br>
</div>
